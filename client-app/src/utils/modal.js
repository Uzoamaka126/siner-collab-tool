import modal from "bootstrap/js/dist/modal";

function activateModal(val, type) {
    var myModal = new bootstrap.Modal(document.getElementById('myModal'), {
        keyboard: false
    })
    if (type === 'hide') {
        
    }
}
